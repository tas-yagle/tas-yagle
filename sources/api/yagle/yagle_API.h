#include <stdio.h>

void yagle (char *figname);

/*
  MAN yagle
  DESCRIPTION
  yagle (<figname>) generates a RTL behavioral description from the transistor-level netlist given by <figname>.
  The netlist should exist in the program's memory (loaded with avt_LoadFile for example).
  RETURN VALUE
  none.
  ERRORS
  none.
*/
